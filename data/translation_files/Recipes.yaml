# yaml-language-server: $schema=../../.schemas/translation_files.json

version: 1
name: Recipes
description: Translations for the craftRecipe scripts. The key needs to be the ID
  of the craftRecipe block.
fileName: Recipes
function: getRecipeDisplayName
patternProperties:
- pattern: '^[A-Za-z0-9_\(\): -]+$'
- pattern: ^\$
